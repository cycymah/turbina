// Тут  будет изначальный контекст, который потом можно будет прокидывать в компоненты
import { createContext } from 'react';
// import songList from '../constants/songsList';

export const ContextSongsData = createContext();
